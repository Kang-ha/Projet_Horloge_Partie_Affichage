/**
 * =====================
 *      test_mqtt.h
 * =====================
 * 
 * Ce fichier déclare la fonction `testMQTT()`, 
 * utilisée pour tester la connexion au broker MQTT.
 * 
 * La définition de cette fonction se trouve dans `test_mqtt.cpp`.
 * Ce header permet au programme principal d’y accéder sans connaître les détails internes.
 */

#ifndef TEST_MQTT_H     // Protection contre les inclusions multiples
#define TEST_MQTT_H

// Déclaration de la fonction de test MQTT (implémentée dans test_mqtt.cpp)
void testMQTT();

#endif  // Fin de la protection d’en-tête
